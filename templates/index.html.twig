<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

</head>
<body>
    <main class="container">
        <div class="row">
            <section class="col-12">
        <h1>{{ title }}</h1>
    
        <table class"table">
            <thead>
                <th>ID</th>
                <th>Email</th>
            </thead>
            <tbody>
                <tr>
                    {% for user in users %}
                    <td>{{ user.id }}</td>
                    <td>{{ user.email }}</td>
                    <td><a href="details.php?id={{ user.id }}">Voir</a> <a href="edit.php?id={{ user.id }}">Modifier</a> <a href="remove.php?id={{ user.id }}">Supprimer</a></td>
                    {% endfor %}
                </tr>
            </tbody>
        </table>
        </section>
        </div>
    </main>

                        

</body>
</html>